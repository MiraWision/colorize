enum ColorFormat {
  HEX = 'hex',
  HEXA = 'hexa',
  RGB = 'rgb',
  RGBA = 'rgba',
  HSL = 'hsl',
  HSLA = 'hsla',
  CMYK = 'cmyk',
  HSV = 'hsv',
}

const BaseColorFormat = ColorFormat.RGBA;

export {
  ColorFormat,
  BaseColorFormat,
};
